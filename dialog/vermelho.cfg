#
# Run-time configuration file for dialog
#
# Automatically generated by "dialog --create-rc <file>"
#
#
# Types of values:
#
# Number     -  <number>
# String     -  "string"
# Boolean    -  <ON|OFF>
# Attribute  -  (foreground,background,highlight?,underline?,reverse?)
#
# dialog --create-rc ~/.dialogrc

# Set aspect-ration.
aspect = 0

# Set separator (for multiple widgets output).
separate_widget = ""

# Set tab-length (for textbox tab-conversion).
tab_len = 0

# Make tab-traversal for checklist, etc., include the list.
visit_items = OFF

# Shadow dialog boxes? This also turns on color.
use_shadow = OFF

# Turn color support ON or OFF
use_colors = ON

# Screen color
screen_color = (RED,BLACK,ON)

# Shadow color
shadow_color = (BLACK,BLACK,ON)

# Dialog box color
dialog_color = (BLACK,RED,OFF)

# Dialog box title color
title_color = (BLACK,RED,OFF)

# Dialog box border color
border_color = (BLACK,RED,OFF)

# Active button color
button_active_color = (BLACK,RED,OFF)

# Inactive button color
button_inactive_color = (BLACK,RED,OFF)

# Active button key color
button_key_active_color = (RED,BLACK,OFF)

# Inactive button key color
button_key_inactive_color = (BLACK,RED,OFF)

# Active button label color
button_label_active_color = (RED,BLACK,OFF)

# Inactive button label color
button_label_inactive_color = (BLACK,RED,OFF)

# Input box color
inputbox_color = (RED,BLACK,ON)

# Input box border color
inputbox_border_color = (RED,BLACK,ON)

# Search box color
searchbox_color = (RED,BLACK,ON)

# Search box title color
searchbox_title_color = (RED,BLACK,OFF)

# Search box border color
searchbox_border_color = (RED,BLACK,OFF)

# File position indicator color
position_indicator_color = (BLACK,RED,OFF)

# Menu box color
menubox_color = (RED,BLACK,OFF)

# Menu box border color
menubox_border_color = (RED,BLACK,OFF)

# Item color
item_color = (RED,BLACK,OFF)

# Selected item color
item_selected_color = (BLACK,RED,OFF)

# Tag color
tag_color = (RED,BLACK,OFF)

# Selected tag color
tag_selected_color = (BLACK,RED,OFF)

# Tag key color
tag_key_color = (RED,BLACK,OFF)

# Selected tag key color
tag_key_selected_color = (BLACK,RED,OFF)

# Check box color
check_color = (RED,BLACK,OFF)

# Selected check box color
check_selected_color = (BLACK,RED,OFF)

# Up arrow color
uarrow_color = (RED,BLACK,ON)

# Down arrow color
darrow_color = (RED,BLACK,ON)

# Item help-text color
itemhelp_color = (WHITE,BLACK,OFF)

# Active form text color
form_active_text_color = (RED,BLACK,ON)

# Form text color
form_text_color = (WHITE,CYAN,ON)

# Readonly form item color
form_item_readonly_color = (CYAN,WHITE,ON)

# Dialog box gauge color
gauge_color = title_color

# Dialog box border2 color
border2_color = dialog_color

# Input box border2 color
inputbox_border2_color = dialog_color

# Search box border2 color
searchbox_border2_color = dialog_color

# Menu box border2 color
menubox_border2_color = dialog_color

